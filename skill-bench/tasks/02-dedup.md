# Tarefa: deduplicar CSV

## Prompt

Crie um arquivo `main.py` em Python que lê um CSV chamado `dados.csv` (colunas: email,nome) e escreve `limpo.csv` sem linhas com email duplicado, mantendo a primeira ocorrência. Gere também um `dados.csv` de exemplo com pelo menos 6 linhas incluindo duplicatas. Inclua um self-check em `__main__` que valida o resultado, e termine executando `python main.py` com sucesso.

## Check

python main.py
